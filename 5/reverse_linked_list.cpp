/*
Reverse a singly linked list.

click to show more hints.

Hint:
A linked list can be reversed either iteratively or recursively. Could you implement both?
*/

#include "header.h"

int main(){
    int arr[] = {0, 1, 2, 3, 4, 5};
    ListNode* head = create_list(arr, arr+sizeof(arr)/sizeof(arr[0]));

    return 0;
}